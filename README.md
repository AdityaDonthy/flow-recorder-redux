# A web app to record time taken for daily tasks

## Project Details

This app was build using React with Typescript and Redux and firebase-firestore for storing data. This app uses Hooks and Redux duck pattern.


